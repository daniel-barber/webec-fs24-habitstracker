{# @pebvariable name="title" type="java.lang.String" #}

{% extends "../layout/default" %}

{% import "forms/input" %}
{% import "forms/submit" %}

    {% block content %}
        <form method="post" class="form">
            {{ input("habit", "Name", "name", habit.name) }}
            {{ input("habit", "Description", "description", habit.description) }}
            {{ submit("Save") }}
        </form>
    {% endblock %}
